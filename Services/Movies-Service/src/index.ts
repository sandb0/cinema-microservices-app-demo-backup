// import { EventEmitter } from 'events'

import { mysqlSettings, mongodbSettings } from './config/config'

import { MySQLPoolConnection } from './database/MySQL'
import { MongoDBConnection } from './database/MongoDB'

// const mysqlConnection = MySQLPoolConnection.connect(mysqlSettings)
// const mongodbConnection = MongoDBConnection.connect(mongodbSettings)
